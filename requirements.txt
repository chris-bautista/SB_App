fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
pydantic-settings==2.7.1

# Usado por exportar_estatico.py (TestClient) para generar docs/.
httpx==0.28.1

# Se usan a partir del módulo de administración (base de datos y exportaciones).
SQLAlchemy==2.0.36
openpyxl==3.1.5
passlib[bcrypt]==1.7.4
# passlib 1.7.4 no es compatible con bcrypt>=4.1 (falla al hashear con
# "password cannot be longer than 72 bytes" por un bug interno de passlib
# detectando la versión de bcrypt). Fijar la versión evita ese error.
bcrypt==4.0.1
itsdangerous==2.2.0
